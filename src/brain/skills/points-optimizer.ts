import { ChatMessage } from '../../crm/conversation'

// Stub — real logic TBD post-MVP
export async function runPointsOptimizerSkill(_history: ChatMessage[]): Promise<string> {
  return 'Points optimization is coming soon! Stay tuned for smart miles & rewards recommendations tailored to your trip.'
}
